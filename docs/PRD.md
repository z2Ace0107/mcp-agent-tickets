# PRD: Super Agent — 基于 MCP 的多智能体工业协管平台

> **版本**: v3.0 | **更新日期**: 2026-05-12
>
> 本项目已从 MVP 阶段演进到 v3.0。当前定位为「基于 MCP 协议的多智能体工业协管平台」，面向制造业工单管理场景，支持 9 个 MCP 工具、意图路由、RAG 知识库、主动监控与告警、混合记忆策略。以下原始 MVP 需求文档记录了项目的起点和演进方向。

---

## v3.0 核心功能清单

### 能力矩阵

| 领域 | 功能 | 说明 |
|------|------|------|
| 智能路由 | 意图分类 + 问题改写 | 5 类意图，3 档路由（chat/simple_query/complex），闲聊省 60% Token |
| 工具矩阵 | 9 个 MCP 工具 | 查询/分析/分配/更新/回复/详情/RAG检索/智能推荐/联网搜索 |
| RAG 知识库 | ChromaDB 向量检索 | 阿里百炼 Embedding，7 条已解决工单索引 |
| 安全防护 | 重试/超时/熔断/降级 | 指数退避 200→800ms，10s 超时，3 次失败熔断 |
| 证据预算 | 按工具裁剪输出 | search 1500/query 2000/web 1200 字符上限 |
| 混合记忆 | 滑动窗口 + 摘要压缩 | 超 10 条时 LLM 压缩早期消息，保留最近 4 条原文 |
| 推理可视化 | 步骤 0 + 工具元信息 | 意图识别/路由徽标/耗时/字符数/裁剪标记 |
| 对话管理 | SQLite 持久化 | 置顶/分组/搜索/重命名/一键备注/导出 Markdown |
| 主动监控 | 紧急告警 + 日报生成 | 侧边栏实时提醒，一键生成结构化日报 |
| MCP 协议 | JSON-RPC stdio | 标准 MCP 服务器，9 个工具定义 |
| 种子数据 | 20 条真实工厂工单 | 设备型号/产线编号/根因分析/经济损失/影响范围 |

### 界面特性

- DeepSeek 极简风格深色主题（#1b1c21）
- 路由徽标（💬 直接回复 / ⚡ 快速查询 / 🧠 深度推理）
- 推理面板：步骤 0 意图识别 + 工具耗时/裁剪/降级标记
- 侧边栏：提醒/工具箱/上下文状态指示器/对话历史管理

---

## 附录: 原始 MVP 需求文档（v1.0）

> 以下为项目起点（2026-05-08）的原始需求文档，保留用于展示演进过程。

### 项目定位

基于MCP协议的标准化Agent系统，演示AI Agent如何通过标准协议调用企业内部业务工具，完成智能工单查询与分析。该项目直接对标企业AI Agent开发岗位。

### 核心用户故事

1. 作为用户，通过自然语言向Agent提问（如"查询最近一周的退款工单"）。
2. Agent能自主理解意图，选择并调用合适的MCP工具。
3. 可完整看到Agent的推理过程（Thought-Action-Observation）。

### MVP功能

**F1：MCP工具服务层**
- 实现两个工具：query_tickets（工单查询）、analyze_tickets（工单分析）。
- 工具使用模拟的企业工单数据。
- 工具服务遵循MCP协议基本规范。

**F2：Agent智能调度层**
- 使用LangChain Agent完成意图识别与工具调度。
- Agent根据自然语言输入自主决定调用哪个工具，支持多轮对话。

**F3：ReAct推理可视化**
- 在前端展示Thought（思考）、Action（动作）、Observation（观察）步骤。

**F4：前端聊天界面**
- 使用Streamlit构建。
- 主区域展示对话历史。
- 侧边栏展示推理步骤和工具调用详情。

### 验收标准

- 输入"最近一周有哪些退款工单？"，Agent调用query_tickets并返回结果。
- 输入"帮我分析一下这个月工单的趋势"，Agent调用analyze_tickets并给出统计结论。
- 输入"你好"，Agent不调用任何工具，直接回复问候语。
