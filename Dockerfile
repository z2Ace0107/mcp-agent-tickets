FROM python:3.10-slim

WORKDIR /app

# TODO: 复制依赖文件并安装
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# TODO: 复制项目代码
# COPY . .

# TODO: 暴露端口与启动指令
# EXPOSE 8000 8501
# CMD ["uvicorn", "backend.mcp_server:app", "--host", "0.0.0.0", "--port", "8000"]
